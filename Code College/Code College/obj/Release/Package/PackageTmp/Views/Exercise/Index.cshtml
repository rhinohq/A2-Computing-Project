@using System.Web.Optimization;
@using System.Data.Entity;
@using Code_College.Models;

@{
    ExDBEntities ExDB = new ExDBEntities();
    UserDBEntities UserDB = new UserDBEntities();
    HttpCookie Cookie = Request.Cookies["UserAuth"];

    if (Cookie != null)
    {
        if (!Account.VerifyCookie(Cookie))
        {
            Response.Redirect("/Login");
        }
    }
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @*@Styles.Render("~/bundles/css")*@
    @Scripts.Render("~/bundles/js")

    <script type="text/javascript">
        $(function () {
            var Console = $.connection.ConsoleHub;

            Console.client.UpdateConsole = function (ConsoleOutput) {

            };

            $.connection.hub.start().done(function () {

            });
        });
    </script>
</head>

<body>
    <div>
        This section, Exercise, is under construction. Please, go to <a href="/Login">Login</a> instead.
    </div>
</body>
</html>